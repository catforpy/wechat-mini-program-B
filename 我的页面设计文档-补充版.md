# 商户端"我的"页面 - 补充功能设计文档

> 项目名称：唐极课得 - 商户端
> 页面名称：我的（Profile）- 补充版本
> 版本：v1.1
> 更新日期：2025-02-09

---

## 📋 新增核心功能概述

### 1. 订单支付信息
显示用户购买模板的订单及支付状态

### 2. 小程序审核状态
显示购买后创建的真实小程序的审核情况

### 3. 小程序开发进度
跟踪小程序的开发状态，不同状态对应不同交互

---

## 🎯 功能一：订单支付信息

### 业务流程

```
用户购买模板
    ↓
创建订单（状态：待支付）
    ↓
用户支付（状态：待审核）
    ↓
平台审核（状态：审核中）
    ↓
审核通过（状态：已完成）
    ↓
创建小程序实例
```

### 订单状态定义

| 状态码 | 状态名称 | 说明 | 可操作 |
|--------|---------|------|--------|
| `unpaid` | 待支付 | 订单已创建，等待支付 | 去支付、取消订单 |
| `pending` | 待审核 | 已支付，等待平台审核 | 查看详情、补充资料 |
| `approved` | 审核通过 | 审核通过，正在创建小程序 | 查看进度 |
| `rejected` | 审核拒绝 | 审核未通过 | 查看原因、重新提交 |
| `completed` | 已完成 | 小程序已创建完成 | 查看小程序 |
| `cancelled` | 已取消 | 订单已取消 | 重新购买 |
| `refunded` | 已退款 | 订单已退款 | 查看退款详情 |

---

### UI设计

#### 1.1 订单卡片（有订单时）

```
┌─────────────────────────────────────┐
│  我的订单              [查看全部 >] │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  📦 模板订单                 │   │
│  │                             │   │
│  │  模板名称：零售电商模板     │   │
│  │  订单编号：ORDER20250209001  │   │
│  │  购买时间：2025-02-09 14:30  │   │
│  │  支付金额：¥299.00          │   │
│  │                             │   │
│  │  ┌───────────────────────┐ │   │
│  │  │  ⏳ 审核中            │ │   │
│  │  │  预计1-3个工作日完成   │ │   │
│  │  └───────────────────────┘ │   │
│  │                             │   │
│  │  [查看详情] [联系客服]      │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 1.2 订单卡片（无订单时）

```
┌─────────────────────────────────────┐
│  我的订单              [查看全部 >] │
├─────────────────────────────────────┤
│                                     │
│        📦                         │
│     还没有订单                     │
│     快去选购模板吧                 │
│                                     │
│      [去选购模板]                  │
│                                     │
└─────────────────────────────────────┘
```

---

### 数据结构

```typescript
// 订单信息
interface Order {
  orderId: string                   // 订单ID
  orderNo: string                   // 订单编号（用户可见）
  templateId: string                // 模板ID
  templateName: string              // 模板名称
  templateIcon: string              // 模板图标
  orderAmount: number               // 订单金额（分）
  actualAmount: number              // 实际支付金额（分）
  status: OrderStatus               // 订单状态
  paymentTime?: string              // 支付时间
  approvalTime?: string             // 审核通过时间
  rejectReason?: string             // 拒绝原因
  estimatedCompletionTime?: string  // 预计完成时间
  createdAt: string                 // 创建时间
  updatedAt: string                 // 更新时间
}

// 订单状态
type OrderStatus =
  | 'unpaid'       // 待支付
  | 'pending'      // 待审核
  | 'approved'     // 审核通过
  | 'rejected'     // 审核拒绝
  | 'completed'    // 已完成
  | 'cancelled'    // 已取消
  | 'refunded'     // 已退款
```

---

### 状态卡片样式

```scss
// 不同状态的订单卡片颜色
.order-card {
  &.status-unpaid {
    border-left: 4rpx solid #ff9500; // 橙色
    .status-badge { background: #fff3e0; color: #ff9500; }
  }

  &.status-pending {
    border-left: 4rpx solid #2196f3; // 蓝色
    .status-badge { background: #e3f2fd; color: #2196f3; }
  }

  &.status-approved {
    border-left: 4rpx solid #9c27b0; // 紫色
    .status-badge { background: #f3e5f5; color: #9c27b0; }
  }

  &.status-completed {
    border-left: 4rpx solid #07c160; // 绿色
    .status-badge { background: #e8f5e9; color: #07c160; }
  }

  &.status-rejected {
    border-left: 4rpx solid #ff3b30; // 红色
    .status-badge { background: #ffebee; color: #ff3b30; }
  }
}
```

---

## 🎯 功能二：小程序审核状态

### 业务流程

```
订单审核通过
    ↓
创建小程序实例（状态：初始化中）
    ↓
提交小程序资料（状态：待提交审核）
    ↓
用户提交审核（状态：审核中）
    ↓
平台审核小程序（状态：审核中）
    ↓
┌───┴────┐
↓         ↓
审核通过  审核拒绝
↓         ↓
进入开发  修改后重审
```

### 小程序审核状态定义

| 状态码 | 状态名称 | 说明 | 可操作 |
|--------|---------|------|--------|
| `initializing` | 初始化中 | 小程序实例创建中 | 查看进度 |
| `pending_submit` | 待提交审核 | 资料准备中，待用户提交 | 提交审核 |
| `reviewing` | 审核中 | 平台正在审核 | 查看进度 |
| `approved` | 审核通过 | 审核通过，进入开发 | 查看开发进度 |
| `rejected` | 审核拒绝 | 审核未通过 | 查看原因、修改资料 |
| `suspended` | 已暂停 | 小程序被暂停 | 查看原因、申诉 |

---

### UI设计

#### 2.1 小程序卡片（审核中）

```
┌─────────────────────────────────────┐
│  我的小程序            [管理小程序 >] │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  ┌──┐                       │   │
│  │  │📱│  张三的零售商城        │   │
│  │  └──┘                       │   │
│  │                             │   │
│  │  ┌───────────────────────┐ │   │
│  │  │  ⏳ 审核中            │ │   │
│  │  │  已提交2天            │ │   │
│  │  └───────────────────────┘ │   │
│  │                             │   │
│  │  [查看进度] [撤回申请]      │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 2.2 小程序卡片（审核通过）

```
┌─────────────────────────────────────┐
│  我的小程序            [管理小程序 >] │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  ┌──┐                       │   │
│  │  │📱│  张三的零售商城        │   │
│  │  └──┘                       │   │
│  │                             │   │
│  │  ┌───────────────────────┐ │   │
│  │  │  ✓ 审核通过            │ │   │
│  │  │  已通过审核            │ │   │
│  │  └───────────────────────┘ │   │
│  │                             │   │
│  │  [进入管理]                │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

#### 2.3 小程序卡片（审核拒绝）

```
┌─────────────────────────────────────┐
│  我的小程序            [管理小程序 >] │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  ┌──┐                       │   │
│  │  │📱│  张三的零售商城        │   │
│  │  └──┘                       │   │
│  │                             │   │
│  │  ┌───────────────────────┐ │   │
│  │  │  ❌ 审核未通过         │ │   │
│  │  │  名称不符合规范        │ │   │
│  │  └───────────────────────┘ │   │
│  │                             │   │
│  │  [查看原因] [修改重审]      │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

### 数据结构

```typescript
// 小程序信息
interface MiniProgram {
  miniProgramId: string            // 小程序ID
  name: string                     // 小程序名称
  icon: string                     // 小程序图标
  description: string              // 描述
  status: MiniProgramStatus        // 审核状态
  developmentStatus: DevelopmentStatus  // 开发状态
  auditStatus?: AuditStatus        // 详细审核状态
  submitTime?: string              // 提交审核时间
  auditTime?: string               // 审核时间
  rejectReason?: string            // 拒绝原因
  auditProgress?: AuditProgress    // 审核进度
  createdAt: string                // 创建时间
  updatedAt: string                // 更新时间
}

// 小程序审核状态
type MiniProgramStatus =
  | 'initializing'     // 初始化中
  | 'pending_submit'   // 待提交审核
  | 'reviewing'        // 审核中
  | 'approved'         // 审核通过
  | 'rejected'         // 审核拒绝
  | 'suspended'        // 已暂停

// 审核进度
interface AuditProgress {
  currentStep: number               // 当前步骤
  totalSteps: number               // 总步骤数
  steps: AuditStep[]               // 步骤列表
}

interface AuditStep {
  step: number                     // 步骤序号
  name: string                     // 步骤名称
  status: 'pending' | 'processing' | 'completed' | 'failed'
  message?: string                 // 步骤说明
  completedAt?: string             // 完成时间
}
```

---

## 🎯 功能三：小程序开发状态

### 业务流程

```
审核通过
    ↓
开始开发（状态：开发中）
    ↓
┌─ 开发阶段分解 ─────────────────┐
│ 1. 环境搭建（状态：初始化）    │
│ 2. 基础配置（状态：配置中）    │
│ 3. 功能开发（状态：开发中）    │
│ 4. 测试验收（状态：测试中）    │
│ 5. 部署上线（状态：部署中）    │
└───────────────────────────────┘
    ↓
开发完成（状态：已就绪）
    ↓
可正常使用（上传商品、配置等）
```

### 开发状态定义

| 状态码 | 状态名称 | 说明 | 可操作功能 |
|--------|---------|------|-----------|
| `not_started` | 未开始 | 尚未开始开发 | 查看计划 |
| `initializing` | 初始化中 | 环境搭建中 | 查看进度 |
| `configuring` | 配置中 | 基础配置中 | 查看进度 |
| `developing` | 开发中 | 功能开发中 | 查看进度、部分功能可用 |
| `testing` | 测试中 | 功能测试中 | 查看进度、部分功能可用 |
| `deploying` | 部署中 | 部署上线中 | 查看进度 |
| `ready` | 已就绪 | 开发完成，可正常使用 | 全部功能可用 |

### 功能权限矩阵

| 功能 | 未开始 | 初始化 | 配置中 | 开发中 | 测试中 | 已就绪 |
|------|--------|--------|--------|--------|--------|--------|
| 查看开发进度 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 查看基础信息 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 编辑小程序资料 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| 上传商品 | ❌ | ❌ | ❌ | ⚠️部分 | ⚠️部分 | ✅ |
| 配置支付 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |
| 查看数据统计 | ❌ | ❌ | ❌ | ⚠️基础 | ⚠️基础 | ✅ |
| 发布小程序 | ❌ | ❌ | ❌ | ❌ | ❌ | ✅ |

---

### UI设计

#### 3.1 开发进度标签

```
┌─────────────────────────────────────┐
│  ┌──┐  📱 张三的零售商城            │
│  │📱│                              │
│  └──┘  ┌───────────────────────┐   │
│        │  🔨 开发中 40%        │   │  ← 右上角标签
│        └───────────────────────┘   │
└─────────────────────────────────────┘
```

**不同状态标签样式**：

| 状态 | 标签文字 | 颜色 | 图标 |
|------|---------|------|------|
| 未开始 | 未开始 | 灰色 #999 | ⏸ |
| 初始化中 | 初始化中 10% | 蓝色 #2196f3 | 🔨 |
| 配置中 | 配置中 30% | 蓝色 #2196f3 | ⚙️ |
| 开发中 | 开发中 60% | 橙色 #ff9500 | 🔨 |
| 测试中 | 测试中 90% | 紫色 #9c27b0 | 🧪 |
| 已就绪 | 已就绪 | 绿色 #07c160 | ✓ |

---

#### 3.2 点击小程序卡片 - 不同状态弹窗

##### A. 审核中状态

```
┌─────────────────────────────────────┐
│         小程序审核状态             │
├─────────────────────────────────────┤
│                                     │
│  ⏳ 当前状态：审核中               │
│                                     │
│  提交时间：2025-02-07 10:30         │
│  已等待：2天                       │
│                                     │
│  预计完成：2025-02-10              │
│                                     │
│  审核内容：                         │
│  • 小程序名称规范                  │
│  • 小程序图标                      │
│  • 小程序描述                      │
│  • 资质文件                        │
│                                     │
│  [查看进度]  [撤回申请]  [联系客服]│
│                                     │
└─────────────────────────────────────┘
```

##### B. 开发中状态

```
┌─────────────────────────────────────┐
│         小程序开发进度             │
├─────────────────────────────────────┤
│                                     │
│  🔨 开发中 - 60%                   │
│  预计完成：2025-02-20              │
│                                     │
│  ┌─────────────────────────────┐   │
│  │  ✓ 1. 环境搭建            │   │
│  │  ✓ 2. 基础配置            │   │
│  │  🔨 3. 功能开发（当前）    │   │
│  │  ⏸ 4. 测试验收            │   │
│  │  ⏸ 5. 部署上线            │   │
│  └─────────────────────────────┘   │
│                                     │
│  当前开发内容：                     │
│  • 商品管理模块                    │
│  • 订单管理模块                    │
│  • 用户中心模块                    │
│                                     │
│  [查看详情]  [联系开发]           │
│                                     │
└─────────────────────────────────────┘
```

##### C. 已就绪状态

```
┌─────────────────────────────────────┐
│         小程序已就绪               │
├─────────────────────────────────────┤
│                                     │
│  ✓ 恭喜！您的小程序已经开发完成   │
│                                     │
│  上线时间：2025-02-15              │
│  运行状态：正常                    │
│                                     │
│  可用功能：                         │
│  ✓ 商品管理                        │
│  ✓ 订单管理                        │
│  ✓ 支付配置                        │
│  ✓ 数据统计                        │
│  ✓ 小程序发布                      │
│                                     │
│  [进入管理后台]                   │
│                                     │
└─────────────────────────────────────┘
```

---

### 数据结构

```typescript
// 开发状态
type DevelopmentStatus =
  | 'not_started'   // 未开始
  | 'initializing'  // 初始化中
  | 'configuring'   // 配置中
  | 'developing'    // 开发中
  | 'testing'       // 测试中
  | 'deploying'     // 部署中
  | 'ready'         // 已就绪

// 开发进度
interface DevelopmentProgress {
  currentPhase: number              // 当前阶段
  totalPhases: number               // 总阶段数
  progress: number                  // 进度百分比 0-100
  estimatedCompletionTime?: string   // 预计完成时间
  phases: DevelopmentPhase[]        // 阶段列表
}

// 开发阶段
interface DevelopmentPhase {
  phase: number                     // 阶段序号
  name: string                     // 阶段名称
  status: 'pending' | 'in_progress' | 'completed' | 'failed'
  description: string              // 阶段描述
  progress: number                 // 阶段进度 0-100
  startedAt?: string               // 开始时间
  completedAt?: string             // 完成时间
  tasks?: DevelopmentTask[]        // 任务列表（可选）
}

// 开发任务
interface DevelopmentTask {
  taskId: string                   // 任务ID
  name: string                     // 任务名称
  status: 'pending' | 'in_progress' | 'completed' | 'failed'
  description?: string             // 任务描述
  completedAt?: string             // 完成时间
}
```

---

## 🎯 完整页面布局（更新后）

```
┌─────────────────────────────────────┐
│  状态栏（自适应）                    │
├─────────────────────────────────────┤
│  ┌───────────────────────────────┐  │
│  │  用户头像 + 基本信息          │  │  ← 头部卡片
│  │  认证状态 + 主体类型          │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  📦 我的订单                 │  │  ← 订单卡片（NEW）
│  │  ├ 订单信息                 │  │
│  │  ├ 支付金额                 │  │
│  │  └ 订单状态                 │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  📱 我的小程序               │  │  ← 小程序卡片（NEW）
│  │  ├ 小程序图标 + 名称         │  │
│  │  ├ 审核状态标签             │  │
│  │  ├ 开发状态标签             │  │
│  │  └ 进度信息                 │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  资质管理                      │  │  ← 资质卡片
│  │  ├ 营业执照/身份证           │  │
│  │  ├ 法人身份证明             │  │
│  │  └ 认证状态                  │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  功能入口                      │  │  ← 功能列表
│  │  ├ 收货地址                   │  │
│  │  ├ 客服中心                   │  │
│  │  ├ 设置                       │  │
│  │  └ 关于                       │  │
│  └───────────────────────────────┘  │
│                                     │
│  ┌───────────────────────────────┐  │
│  │  退出登录                      │  │
│  └───────────────────────────────┘  │
│                                     │
│  Tab Bar（60px高度）               │
└─────────────────────────────────────┘
```

---

## 🔄 完整交互流程

### 1. 点击小程序卡片 - 状态判断

```
用户点击小程序卡片
    ↓
检查小程序审核状态
    ↓
┌───────────┬───────────┬───────────┬───────────┐
↓           ↓           ↓           ↓
审核中      审核拒绝    审核通过    其他状态
↓           ↓           ↓           ↓
显示审核   显示拒绝   检查开发   提示异常
进度弹窗   原因弹窗   状态        状态
                      ↓
              ┌─────┬─────┬─────┐
              ↓     ↓     ↓     ↓
            未开始  开发中 已就绪 其他
              ↓     ↓     ↓     ↓
            显示   显示   进入 显示
            计划   进度   后台   异常
```

---

### 2. 开发进度详情页

```
┌─────────────────────────────────────┐
│  ← 小程序开发进度                  │
├─────────────────────────────────────┤
│  📱 张三的零售商城                 │
│  🔨 开发中 - 60%                   │
├─────────────────────────────────────┤
│  进度条：████████░░░░░░░░ 60%       │
│  预计完成：2025-02-20               │
├─────────────────────────────────────┤
│  开发阶段：                         │
│  ✓ 1. 环境搭建（100%）            │
│  ✓ 2. 基础配置（100%）            │
│  🔨 3. 功能开发（60%） ← 当前     │
│  ⏸ 4. 测试验收（0%）             │
│  ⏸ 5. 部署上线（0%）             │
├─────────────────────────────────────┤
│  当前任务：                         │
│  🔨 商品管理模块开发              │
│     • 商品列表（✓ 已完成）        │
│     • 商品分类（✓ 已完成）        │
│     • 商品添加（🔨 进行中）        │
│     • 商品编辑（⏸ 未开始）        │
│  ⏸ 订单管理模块                  │
│  ⏸ 用户中心模块                  │
├─────────────────────────────────────┤
│  [返回]  [联系客服]                │
└─────────────────────────────────────┘
```

---

## 📊 完整数据结构（整合）

```typescript
// 完整用户信息
interface UserProfile {
  userId: string
  nickname: string
  avatar: string
  phone: string
  email?: string
  entityType: EntityType
  certificationStatus: CertificationStatus
  certificationTime?: string

  // NEW: 订单信息
  latestOrder?: Order              // 最新订单
  orderCount?: number               // 订单总数

  // NEW: 小程序信息
  miniPrograms?: MiniProgram[]      // 用户的小程序列表
}

// 订单信息
interface Order {
  orderId: string
  orderNo: string
  templateId: string
  templateName: string
  templateIcon: string
  orderAmount: number
  actualAmount: number
  status: OrderStatus
  paymentTime?: string
  approvalTime?: string
  rejectReason?: string
  estimatedCompletionTime?: string
  createdAt: string
  updatedAt: string

  // NEW: 关联的小程序（如果已创建）
  miniProgram?: MiniProgram
}

// 小程序信息（整合审核和开发状态）
interface MiniProgram {
  miniProgramId: string
  name: string
  icon: string
  description: string

  // 审核状态
  status: MiniProgramStatus
  submitTime?: string
  auditTime?: string
  rejectReason?: string
  auditProgress?: AuditProgress

  // 开发状态
  developmentStatus: DevelopmentStatus
  developmentProgress?: DevelopmentProgress

  // 功能权限
  permissions: string[]           // 可用功能列表

  createdAt: string
  updatedAt: string
}

// 审核进度
interface AuditProgress {
  currentStep: number
  totalSteps: number
  steps: AuditStep[]
}

interface AuditStep {
  step: number
  name: string
  status: 'pending' | 'processing' | 'completed' | 'failed'
  message?: string
  completedAt?: string
}

// 开发进度
interface DevelopmentProgress {
  currentPhase: number
  totalPhases: number
  progress: number              // 0-100
  estimatedCompletionTime?: string
  phases: DevelopmentPhase[]
}

interface DevelopmentPhase {
  phase: number
  name: string
  status: 'pending' | 'in_progress' | 'completed' | 'failed'
  description: string
  progress: number              // 0-100
  startedAt?: string
  completedAt?: string
  tasks?: DevelopmentTask[]
}

interface DevelopmentTask {
  taskId: string
  name: string
  status: 'pending' | 'in_progress' | 'completed' | 'failed'
  description?: string
  completedAt?: string
}
```

---

## 🔌 API接口定义（更新）

```typescript
// API: 获取用户完整信息
GET /api/user/profile
Response: UserProfile

// API: 获取订单列表
GET /api/orders?page=1&limit=10
Response: {
  list: Order[]
  total: number
}

// API: 获取订单详情
GET /api/orders/:orderId
Response: Order

// API: 获取用户的小程序列表
GET /api/user/mini-programs
Response: MiniProgram[]

// API: 获取小程序详情
GET /api/mini-programs/:miniProgramId
Response: MiniProgram

// API: 获取小程序审核进度
GET /api/mini-programs/:miniProgramId/audit-progress
Response: AuditProgress

// API: 获取小程序开发进度
GET /api/mini-programs/:miniProgramId/development-progress
Response: DevelopmentProgress

// API: 提交小程序审核
POST /api/mini-programs/:miniProgramId/submit-audit
Body: { auditData: any }
Response: { success: boolean }

// API: 撤回小程序审核
POST /api/mini-programs/:miniProgramId/withdraw-audit
Response: { success: boolean }

// API: 更新小程序资料
PUT /api/mini-programs/:miniProgramId
Body: Partial<MiniProgram>
Response: { success: boolean }

// API: 进入管理后台
POST /api/mini-programs/:miniProgramId/enter-admin
Response: {
  success: boolean
  adminUrl?: string
  token?: string
}
```

---

## 🎨 组件设计（更新）

### 页面组件结构

```
pages/profile/
├── index.vue                      # 主页面
├── components/
│   ├── UserCard.vue               # 用户信息卡片
│   ├── OrderCard.vue              # 订单卡片（NEW）
│   ├── MiniProgramCard.vue        # 小程序卡片（NEW）
│   ├── QualificationCard.vue      # 资质卡片
│   ├── MenuList.vue               # 功能菜单列表
│   └── StatusBadge.vue            # 状态徽章
├── sub-pages/
│   ├── certification/             # 认证流程
│   ├── order-detail/              # 订单详情（NEW）
│   │   └── index.vue
│   ├── mini-program/              # 小程序相关（NEW）
│   │   ├── list.vue              # 小程序列表
│   │   ├── detail.vue            # 小程序详情
│   │   ├── audit-status.vue      # 审核状态弹窗
│   │   ├── development-progress.vue  # 开发进度
│   │   └── admin/                # 管理后台（后续开发）
│   ├── qualification-detail/      # 资质详情
│   └── upload/                    # 上传页面
└── types.ts                       # TypeScript类型定义
```

---

## 📱 组件Props定义（更新）

```typescript
// OrderCard.vue
interface OrderCardProps {
  order: Order
  onViewDetail?: () => void
  onContactService?: () => void
}

// MiniProgramCard.vue
interface MiniProgramCardProps {
  miniProgram: MiniProgram
  showStatus?: boolean             // 是否显示状态标签
  onClick?: (miniProgram: MiniProgram) => void
}

// StatusBadge.vue（增强版）
interface StatusBadgeProps {
  type: 'order' | 'audit' | 'development'
  status: OrderStatus | MiniProgramStatus | DevelopmentStatus
  size?: 'small' | 'medium' | 'large'
  progress?: number                // 进度百分比（可选）
}
```

---

## 🚀 开发优先级（更新）

### P0 - 核心功能（必须完成）
1. ✅ 用户信息展示
2. ✅ 主体类型选择
3. ✅ 企业资质上传
4. ✅ 个人资质上传
5. ✅ 订单信息展示（NEW）
6. ✅ 小程序基本信息展示（NEW）
7. ✅ 审核状态展示（NEW）

### P1 - 重要功能（优先完成）
1. ⚠️ 订单详情查看（NEW）
2. ⚠️ 小程序审核进度查看（NEW）
3. ⚠️ 小程序开发进度查看（NEW）
4. ⚠️ 不同状态弹窗交互（NEW）
5. ⚠️ 境外资质上传
6. ⚠️ 资质查看和更新

### P2 - 增强功能（后续迭代）
1. 📋 小程序管理后台入口
2. 📋 开发进度详情页
3. 📋 订单列表和筛选
4. 📋 消息通知
5. 📋 资质到期提醒

---

## 📌 开发建议

### 阶段一：基础功能（1-2周）
1. 实现用户信息卡片
2. 实现订单卡片（展示最新订单）
3. 实现小程序卡片（基本信息+状态标签）
4. 实现资质卡片

### 阶段二：交互功能（1-2周）
1. 订单详情弹窗/页面
2. 审核状态弹窗
3. 开发进度弹窗
4. 不同状态的路由跳转

### 阶段三：管理功能（后续开发）
1. 小程序管理后台
2. 资质更新功能
3. 订单管理功能

---

**文档版本**: v1.1
**更新内容**: 新增订单支付信息、小程序审核状态、开发进度功能
**最后更新**: 2025-02-09
**维护者**: 开发团队
