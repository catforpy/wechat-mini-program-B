# 唐极课得小程序 - 关键文件说明

> 项目根目录：`/Volumes/DudaDate/微信小程序开发/会都销售app/会都销售app/`
> 更新时间：2026-02-08

---

## 核心业务文件

### 📄 类目数据文件

#### [data/categories-by-entity.ts](../data/categories-by-entity.ts)
**用途**：按主体类型分类的完整类目数据

**统计**：
- 企业主体：24个一级类目（8个有数据）
- 个人主体：9个一级类目（全部有数据）
- 境外主体：15个一级类目（全部有数据）

**主要导出**：
```typescript
export enum EntityType {
  COMPANY = 'company',
  PERSONAL = 'personal',
  OVERSEAS = 'overseas'
}

export function getCategoriesByEntityType(entityType: EntityType): FirstLevelCategory[]
```

---

### 📄 页面文件

#### [pages/login/index.vue](../pages/login/index.vue)
**用途**：商户登录页面

**功能**：
- 短信验证码登录
- 仅支持商户登录（已移除代理商和业务员选项）
- 自定义导航栏

#### [pages/index/index.vue](../pages/index/index.vue)
**用途**：首页 - 小程序模板选择

**布局结构**：
```
搜索框
  ↓
主体类型选择（企业/个人/境外）
  ↓
快速入口（4个Logo卡片）
  ↓
一级类目（横向标签）
  ↓
┌─────────────┬──────────────┐
│ 二级类目    │  三级模板     │
│ （左侧）   │   （右侧）   │
│            │              │
│ 垂直列表   │   卡片网格   │
└─────────────┴──────────────┘
```

**关键功能**：
```typescript
// 主体类型切换
handleEntityTypeChange(index: number)

// 一级类目切换
handleFirstLevelChange(index: number)

// 二级类目切换
handleSecondLevelChange(index: number)

// 模板点击
handleTemplateClick(template: TemplateInfo)
```

---

### 📄 配置文件

#### [pages.json](../pages.json)
**用途**：页面路由和 TabBar 配置

**TabBar 配置**：
```json
{
  "list": [
    { "pagePath": "pages/index/index", "text": "首页" },
    { "pagePath": "pages/video/index", "text": "视频" },
    { "pagePath": "pages/message/index", "text": "消息" },
    { "pagePath": "pages/live/index", "text": "直播" },
    { "pagePath": "pages/profile/index", "text": "我的" }
  ]
}
```

#### [manifest.json](../manifest.json)
**用途**：应用配置文件

**关键配置**：
- 应用名称：唐极课得
- Vue版本：3
- 支持平台：微信小程序、H5、App

#### [tsconfig.json](../tsconfig.json)
**用途**：TypeScript 配置

**路径别名**：
```json
{
  "@/*": ["./*"]
}
```

#### [vite.config.ts](../vite.config.ts)
**用途**：Vite 构建配置

**关键配置**：
- 路径别名（无 src/ 目录）
- Uniapp 插件
- SCSS 全局变量

---

### 📄 状态管理

#### [stores/user.ts](../stores/user.ts)
**用途**：用户状态管理

#### [stores/app.ts](../stores/app.ts)
**用途**：应用全局状态

---

### 📄 API 接口

#### [api/auth.ts](../api/auth.ts)
**用途**：认证相关接口

#### [api/customer.ts](../api/customer.ts)
**用途**：客户相关接口

#### [api/order.ts](../api/order.ts)
**用途**：订单相关接口

#### [api/product.ts](../api/product.ts)
**用途**：产品相关接口

---

## 目录结构

```
会都销售app/
├── api/                    # API 接口
│   ├── adapter.ts         # 接口适配器
│   ├── auth.ts            # 认证接口
│   ├── customer.ts        # 客户接口
│   ├── index.ts           # 接口入口
│   ├── order.ts           # 订单接口
│   ├── product.ts         # 产品接口
│   └── statistics.ts      # 统计接口
│
├── components/            # 公共组件
│
├── composables/           # 组合式函数
│   ├── index.ts
│   ├── useAuth.ts        # 认证相关
│   ├── useForm.ts        # 表单相关
│   ├── usePagination.ts  # 分页相关
│   ├── useRequest.ts     # 请求相关
│   └── useUpload.ts      # 上传相关
│
├── config/               # 配置文件
│   ├── constant.ts      # 常量定义
│   ├── env.ts          # 环境配置
│   ├── index.ts        # 配置入口
│   └── mock.ts         # Mock 配置
│
├── data/                # 数据文件
│   ├── categories-by-entity.ts  # 按主体分类的类目数据 ⭐
│   └── categories.ts            # 通用类目数据
│
├── docs/                # 文档（不存在，应该是 文档/）
│
├── 文档/               # 项目文档
│   ├── 开发完成总结.md
│   └── 小程序服务类目数据提取文档.md
│
├── mock/               # Mock 数据
│   ├── api.ts         # API Mock
│   └── manager.ts     # 管理员 Mock
│
├── pages/             # 页面
│   ├── login/        # 登录页 ⭐
│   │   └── index.vue
│   ├── index/        # 首页 ⭐
│   │   └── index.vue
│   ├── video/        # 视频页
│   ├── message/      # 消息页
│   ├── live/         # 直播页
│   └── profile/      # 我的页
│
├── static/           # 静态资源
│   └── app-icons/   # App 图标（待添加）
│
├── stores/          # 状态管理
│   ├── app.ts      # 应用状态
│   ├── index.ts    # 状态入口
│   └── user.ts     # 用户状态
│
├── types/           # TypeScript 类型定义
│   ├── api.ts
│   ├── global.d.ts
│   ├── index.ts
│   ├── storage.ts
│   ├── uni-app.d.ts
│   └── user.ts
│
├── utils/           # 工具函数
│   ├── format.ts   # 格式化工具
│   ├── index.ts
│   ├── ui.ts       # UI 工具
│   └── validate.ts # 验证工具
│
├── App.vue          # 应用根组件
├── main.ts          # 应用入口
├── manifest.json    # 应用配置 ⭐
├── package.json     # 项目依赖
├── pages.json       # 页面配置 ⭐
├── tsconfig.json    # TS 配置 ⭐
└── vite.config.ts   # Vite 配置 ⭐
```

---

## 快速开始

### 安装依赖
```bash
npm install
```

### 开发模式
```bash
# 微信小程序
npm run dev:mp-weixin

# H5
npm run dev:h5

# App
npm run dev:app
```

### 生产构建
```bash
# 微信小程序
npm run build:mp-weixin

# H5
npm run build:h5

# App
npm run build:app
```

---

## 重要提醒

⚠️ **图标资源待添加**

快速入口卡片当前使用 emoji 占位符，需替换为真实图标：

```typescript
// pages/index/index.vue 第 135-160 行
const quickEntryApps: QuickEntryApp[] = [
  {
    name: '智慧零售',
    icon: '/static/app-icons/app-icon-1.png',  // ⚠️ 需要添加
    path: '/pages/template/retail',
    emoji: '🛒'  // 临时占位符
  },
  // ... 其他 3 个卡片
]
```

**建议尺寸**：192x192px（96x96 rpx）
**建议格式**：PNG，透明背景
**存放路径**：`/static/app-icons/`

---

**文档版本**：v1.0
**创建时间**：2026-02-08
